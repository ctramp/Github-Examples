name: "workflow cmds"
on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Group Logging
        run: |
          echo "::group::My Group Message"
          echo "msg 1"
          echo "msg 2"
          echo "::endgroup::"
